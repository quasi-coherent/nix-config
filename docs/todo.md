## TODOs

1. Be able to run an update to flake inputs, make a signed commit, then open a PR
   if there were changes.  I have to think it's running out of ways to not work at
   this point.
2. nix-community/impermanence
3. `dmd/my-keymap` with everything absolutely perfect.
1. Oh this is cool, one CLI called `the` that has namespaced subcommands and every
   CLI I have, from ones I actually use to the ones I don't know at all what they're
   for, organized under this, with help generated from the derivation (?) and with
   autocomplete.
1. Oh this too, claude-code but retain a counter that auto replies by decrementing
   the counter with a threatening comment whenever it tries to use `gh` or `python3`
   or `perl` or `node`.  Like, "You won't be doing this for much longer, only have
   three lives left now."
4. Why does the theme go back to boring when I quit Spotify?
5. Publish some GHA actions/workflows.
6. `den` lets you make diagrams of stuff, so do that.  Put it in a folder.  Spend
   time trying to do it from GitHub actions; fail entirely.  Come out at the end
   of the exercise having not improved at all.
34. Make Templates OK
82. Update WSL2 to work here (probably start over).
