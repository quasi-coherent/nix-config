# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/cd.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
concurrency:
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
jobs:
  flake-update:
    name: flake update
    permissions:
      contents: write
      pull-requests: write
    runs-on: macos-26
    steps:
    - uses: actions/checkout@v6
      with:
        ref: unstable
    - uses: cachix/install-nix-action@v30
      with:
        nix_path: nixpkgs=channel:nixos-unstable
    - uses: cachix/cachix-action@v17
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        name: quasi-coherent
    - uses: DeterminateSystems/update-flake-lock@main
      with:
        branch: unstable
        commit-msg: Update flake.lock
        gpg-passphrase: ${{ secrets.PGP_PASSPHRASE }}
        gpg-private-key: ${{ secrets.PGP_PRIVATE_KEY }}
        sign-commits: true
    timeout-minutes: 60
name: cd
'on':
  schedule:
  - cron: 0 0 * * 0
  workflow_dispatch: {}
